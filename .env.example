# Slack Configuration
# Get these from https://api.slack.com/apps → Your App → Basic Information / OAuth & Permissions
SLACK_SIGNING_SECRET=your_signing_secret_here
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# LLM Configuration
# Format: provider/model (e.g., anthropic/claude-haiku-4.5 or anthropic/claude-sonnet-4.5)
LLM_MODEL=anthropic/claude-haiku-4.5
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Report Admins (comma-separated Slack user IDs who can run /team_report)
# Find user IDs in Slack: click on profile → More → Copy member ID
REPORT_ADMINS=U12345678,U87654321

# Optional: Default timezone if user timezone cannot be retrieved
# Uses IANA timezone format (e.g., America/Los_Angeles, Europe/London)
DEFAULT_TIMEZONE=America/Los_Angeles

# Database (automatically set for production on Fly.io)
# DATABASE_URL=/data/production.db

# Logging level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=DEBUG
